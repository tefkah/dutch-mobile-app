
import { Layout } from '@ui-kitten/components';
import * as React from 'react';
import { Text } from 'react-native';
export default function VowelScreen() {
    return (
        <Layout style={{ flex: 1, justifyContent: 'center' }}>
            <Text>Select your options</Text>
        </Layout>
    );
}
